{
  "name": "format-on-save",
  "description": "Automatically format Python and TypeScript files on save",
  "trigger": "onFileSave",
  "conditions": [
    {
      "filePattern": "backend/**/*.py",
      "command": "black --quiet ${file}",
      "errorMessage": "Black formatter not installed. Run: pip install black"
    },
    {
      "filePattern": "frontend/**/*.{ts,tsx}",
      "command": "npx prettier --write ${file}",
      "errorMessage": "Prettier not installed. Run: npm install --save-dev prettier"
    }
  ],
  "timeout": 2000,
  "onError": "notify"
}
